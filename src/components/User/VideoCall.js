
import React from 'react';

export default function VideoCall({ meetingLink }) {
    return (
        <div>
            {/* Video call component implementation */}
            <p>Video call with link: {meetingLink}</p>
        </div>
    );
}
